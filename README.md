# Tact UI Auto Complete

A gui for auto complete widgets.

## Install

Add the following to your `bower.json` dependencies:

`"tact.gui.uiAutoComplete": "git@github.com:colinkahn/tact.gui.uiAutoComplete.git#0.1.0"`
